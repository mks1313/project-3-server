const express = require('express');
const router = express.Router();
const User = require('../models/User.model');
const Restaurant = require('../models/Restaurant.model');


router.post('/');


router.put('/:likestId');


router.delete('/:likestId');



module.exports = router;